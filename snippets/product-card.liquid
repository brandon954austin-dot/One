{%- comment -%}
  Product card with ONE clean price (fixes the old theme's duplicated
  "Regular price ... Regular price Sale price ..." bug) and no bogus
  "Sale Sold out" badges on in-stock items.
  Usage: {% render 'product-card', product: product %}
{%- endcomment -%}
<a href="{{ product.url }}" class="card product-card">
  {%- if product.tags contains 'consult' -%}
    <span class="badge">Custom-built</span>
  {%- endif -%}

  {%- if product.featured_image -%}
    <img
      class="product-card__img"
      src="{{ product.featured_image | image_url: width: 640 }}"
      alt="{{ product.featured_image.alt | default: product.title | escape }}"
      loading="lazy"
      width="640"
      height="480"
    >
  {%- else -%}
    <div class="product-card__img--placeholder" aria-hidden="true">
      <svg width="56" height="56" viewBox="0 0 48 48" fill="none">
        <defs>
          <linearGradient id="amaCardL" x1="14" y1="12" x2="20" y2="32" gradientUnits="userSpaceOnUse">
            <stop offset="0" stop-color="#1d2a9e"/>
            <stop offset="1" stop-color="#2f45f0"/>
          </linearGradient>
          <linearGradient id="amaCardR" x1="34" y1="10" x2="20" y2="32" gradientUnits="userSpaceOnUse">
            <stop offset="0" stop-color="#35de84"/>
            <stop offset="1" stop-color="#2bd473"/>
          </linearGradient>
        </defs>
        <path d="M14 18.5 L20 31" stroke="url(#amaCardL)" stroke-width="13" stroke-linecap="round"/>
        <path d="M33.5 13.5 L20 31" stroke="url(#amaCardR)" stroke-width="13" stroke-linecap="round"/>
      </svg>
    </div>
  {%- endif -%}

  <h3>{{ product.title }}</h3>

  <div class="price">
    {%- if product.compare_at_price > product.price -%}
      {{ product.price | money }} <s>{{ product.compare_at_price | money }}</s>
    {%- elsif product.price_varies -%}
      From {{ product.price_min | money }}
    {%- else -%}
      {{ product.price | money }}
    {%- endif -%}
  </div>

  <p>{{ product.description | strip_html | truncate: 120 }}</p>

  {%- if product.tags contains 'consult' -%}
    <span class="btn btn--outline">Program details</span>
  {%- elsif product.available -%}
    <span class="btn btn--outline">View &amp; buy</span>
  {%- else -%}
    <span class="btn btn--outline">Sold out</span>
  {%- endif -%}
</a>
